let ws;const send=x=>ws.send(JSON.stringify(x));ws=new WebSocket((location.protocol==='https:'?'wss':'ws')+'://'+location.host);ws.onopen=()=>send({type:'CREATE_ROOM',name:'Host'});ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.type==='ROOM_CREATED'){room.innerHTML='<h2>ROOM '+m.code+'</h2>';start.hidden=false}if(m.type==='STATE'){players.innerHTML=m.players.map(p=>`<div>${p.name} — ${p.character} ${p.connected?'🟢':'⚪'}</div>`).join('');if(m.game)game.innerHTML=`<h2>ROUND ${m.game.round}</h2><h3>${m.game.currentColor||''}</h3>`}};start.onclick=()=>send({type:'START_GAME'});